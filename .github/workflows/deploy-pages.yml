name: Deploy SafeQuest to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Download SafeQuest
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install project packages
        run: npm ci

      - name: Build SafeQuest
        run: GITHUB_PAGES=true npm run build:github-pages

      - name: Prepare GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload SafeQuest
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Publish SafeQuest
        id: deployment
        uses: actions/deploy-pages@v4
